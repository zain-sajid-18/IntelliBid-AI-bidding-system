/**
 * Auth layout — minimal layout for login/register pages.
 * No sidebar, no role-based navigation.
 */
export default function AuthLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
